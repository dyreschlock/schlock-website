<t:container
        xmlns:t="http://tapestry.apache.org/schema/tapestry_5_3.xsd"
>
    <div class="panel">
        <table class="platformData">
            <tr>
                <th class="name outerTop outerRight outerLeft">
                    ${platformGroup}
                </th>
            </tr>

            <t:loop source="consoleData" value="currentConsole" index="currentIndex">
                <tr>
                    <td class="name ${outerLeftCss} ${currentConsole.code}"><a href="/apps/games/${currentConsole.code}">${currentConsole.name}</a></td>
                    <td class="count ${outerRightCss} ${evenOdd}">${currentConsoleCount}</td>
                </tr>
            </t:loop>

            <tr>
                <td class="empty"></td>
                <td class="totalConsoleCount outerRight outerLeft outerBottom">
                    ${totalCount}
                </td>
            </tr>

        </table>
    </div>
</t:container>